# Use DEV Supabase project for local / staging. Never put production keys here.
# See docs/ENVIRONMENTS_AND_SUPABASE.md for dev vs production setup.
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
# Local: http://localhost:3000  |  Production: set in Vercel only (e.g. https://b2bmobiles.ca)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: run migrations from CLI (npm run migrate). Get from Supabase Dashboard → Settings → Database → Connection string (URI).
# Use DEV project URL here. Never use production DB URL in .env.
DATABASE_URL=postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:5432/postgres